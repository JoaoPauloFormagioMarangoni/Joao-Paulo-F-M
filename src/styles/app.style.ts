import styled from 'styled-components'

export const Container = styled.div`
    width: 100vw;
    height: 100vh;

    background: linear-gradient(90deg, var(--background) 12.29%, var(--orange) 100%);

    display: flex;
    align-items: center;
    justify-content: space-around;
`
